def send_onboarding_email(employee_name: str) -> None:
    print(f"Sending onboarding email to {employee_name}...")


def create_payroll_record(employee_name: str) -> None:
    print(f"Creating payroll record for {employee_name}...")
